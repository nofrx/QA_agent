Phase 1 - ENGINEERING - Iterations 1 to 5: Fix and harden the core pipeline. Work in /Users/tomislavbelacic/shoe-qa/. Use engineering-skills:engineering-team skill. Run all tests first. Fix any failing tests. Then iterate: read each backend module, each blender script, the pipeline orchestrator, and the FastAPI routes. Fix bugs, add error handling for real-world edge cases like missing textures, failed Blender runs, network timeouts, malformed GLB files. Ensure the AppleScript Chrome integration works reliably. After each fix, run tests. Add missing tests for downloader, pipeline, and blender_runner. Commit after each meaningful fix.

Phase 2 - QA VALIDATION - Iterations 6 to 10: Act as a 3D artist QA agent. Start the server with python3 -m uvicorn backend.main:app --port 8080. Test with real SKU F5714D05U-K11 using the auto-fetch. Review every measurement we output: Are flipped normals detected correctly or is the algorithm too sensitive for scanned meshes? Is UV overlap detection meaningful? Are texture diff percentages useful or noisy? For each metric ask: what does this tell the QA artist, is it actionable, should we threshold or filter it differently? Fix the normal detection to use a smarter heuristic comparing against local neighborhood not global center. Add severity levels critical/warning/info to issues. Make texture diff stats concise showing only meaningful changes above noise threshold. Reduce report size by resizing embedded images to 1024px max. Test the HTML report renders correctly in browser.

Phase 3 - UX POLISH - Iterations 11 to 15: Open http://localhost:8080 and test the full UX flow as a user. Fix every rough edge: progress messages should be clear and timestamped, errors should be human-readable not stack traces, the reports library should show brand/color/date clearly, report HTML should load fast and look professional. Add a loading state when fetching reports list. Make the URL mode helper snippet actually work by testing it in browser console on dashboard. Ensure the report template has proper image zoom, collapsible sections work, and stats tables are scannable at a glance. Every data point in the report should have a label explaining what it means and why it matters.

Phase 4 - FINAL REVIEW - Iterations 16 to 20: Run full end-to-end test with 2 different SKUs. Use superpowers:requesting-code-review skill to review all code. Fix any issues found. Run all tests, ensure 100 percent pass. Check git log is clean with descriptive commits. Verify report HTML is self-contained and shareable. Do a final UX walkthrough: enter SKU, watch progress, open report, verify every section has real data and looks correct. If anything is broken or unclear, fix it. The goal is production-ready quality.
